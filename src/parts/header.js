import React from 'react';
import logo from "../images/logo.png"

function Header() {
    return (
        <header>
            <nav>
            </nav>
            <h2>header</h2>
        </header>
    );
}

export default Header;